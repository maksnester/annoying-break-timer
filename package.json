{
  "name": "annoying-break-timer",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build --target universal-apple-darwin",
    "dev": "tauri dev",
    "test": "vitest run && cd src-tauri && cargo test",
    "test:watch": "vitest",
    "bump": "node scripts/bump-version.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "jsdom": "^25",
    "vitest": "^2"
  }
}
